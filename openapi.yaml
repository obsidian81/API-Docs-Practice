openapi: 3.1.0
info:
  title: User Management API
  description: A production-ready API for managing user accounts and roles.
  version: 1.3.0
  contact:
    name: API Support
    email: support@example.com

tags:
  - name: Users
    description: Operations related to user management

servers:
  - url: https://api.example.com/v1
    description: Primary Production Server

security:
  - BearerAuth: []

webhooks:
  userCreated:
    post:
      summary: User Created Event
      description: |
        Triggered when a new user account is successfully registered. 
        Your server should respond with `200 OK` to acknowledge receipt.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserCreatedEvent'
      responses:
        '200':
          description: Event received successfully.

paths:
  /users:
    get:
      summary: List all users
      description: Retrieve a paginated list of registered users.
      operationId: listUsers
      tags:
        - Users
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/LimitParam'
      responses:
        '200':
          description: A paginated list of users retrieved successfully.
          headers:
            X-RateLimit-Limit:
              $ref: '#/components/headers/X-RateLimit-Limit'
            X-RateLimit-Remaining:
              $ref: '#/components/headers/X-RateLimit-Remaining'
            X-RateLimit-Reset:
              $ref: '#/components/headers/X-RateLimit-Reset'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUserList'
        '401':
          $ref: '#/components/responses/401Unauthorized'

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Enter your JWT token to authorize requests.

  headers:
    X-RateLimit-Limit:
      description: The maximum number of requests allowed in the current time window.
      schema:
        type: integer
        example: 100
    X-RateLimit-Remaining:
      description: The number of remaining requests allowed within the current window.
      schema:
        type: integer
        example: 85
    X-RateLimit-Reset:
      description: The time at which the current rate limit window resets (Unix timestamp).
      schema:
        type: integer
        example: 1693000000

  parameters:
    PageParam:
      name: page
      in: query
      description: Page number to retrieve (1-indexed).
      required: false
      schema:
        type: integer
        default: 1
        minimum: 1
    LimitParam:
      name: limit
      in: query
      description: Number of items to return per page.
      required: false
      schema:
        type: integer
        default: 20
        maximum: 100

  responses:
    401Unauthorized:
      description: Unauthorized - Token missing or invalid.
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    BaseUser:
      type: object
      required:
        - id
        - name
        - email
        - user_type
      properties:
        id:
          type: string
          format: uuid
          example: "123e4567-e89b-12d3-a456-426614174000"
        name:
          type: string
          example: "Alex Mercer"
        email:
          type: string
          format: email
          example: "alex.mercer@example.com"
        user_type:
          type: string
          example: "developer"

    AdminUser:
      allOf:
        - $ref: '#/components/schemas/BaseUser'
        - type: object
          required:
            - admin_level
            - permissions
          properties:
            admin_level:
              type: integer
              example: 1
            permissions:
              type: array
              items:
                type: string
              example: ["users:read", "users:write", "billing:manage"]

    DeveloperUser:
      allOf:
        - $ref: '#/components/schemas/BaseUser'
        - type: object
          required:
            - github_handle
            - api_key_limit
          properties:
            github_handle:
              type: string
              example: "alexm-dev"
            api_key_limit:
              type: integer
              example: 5

    UserProfile:
      type: object
      discriminator:
        propertyName: user_type
        mapping:
          admin: '#/components/schemas/AdminUser'
          developer: '#/components/schemas/DeveloperUser'
      oneOf:
        - $ref: '#/components/schemas/AdminUser'
        - $ref: '#/components/schemas/DeveloperUser'

    UserCreatedEvent:
      type: object
      required:
        - id
        - event
        - timestamp
        - data
      properties:
        id:
          type: string
          format: uuid
          example: "a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11"
        event:
          type: string
          example: "user.created"
        timestamp:
          type: string
          format: date-time
          example: "2026-08-26T12:00:00Z"
        data:
          $ref: '#/components/schemas/UserProfile'

    PaginatedUserList:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/UserProfile'
        pagination:
          type: object
          properties:
            total_count:
              type: integer
              example: 150
            page:
              type: integer
              example: 1
            limit:
              type: integer
              example: 20
            has_more:
              type: boolean
              example: true

    ErrorResponse:
      type: object
      properties:
        code:
          type: integer
          example: 401
        message:
          type: string
          example: "Authentication token missing or invalid."